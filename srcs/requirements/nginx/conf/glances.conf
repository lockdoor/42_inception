server {
    listen 61208;
    listen [::]:61208;

    # root /var/www/html;

    # index index.php;

    server_name ${DOMAIN_NAME};

    location / {
        proxy_pass http://glances:61208;
    }
}
